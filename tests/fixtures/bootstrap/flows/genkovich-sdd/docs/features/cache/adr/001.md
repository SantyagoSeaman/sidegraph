# Cache ADR

## Context
Repeated reads dominate latency.

## Decision Outcome
Use an in-process cache in `src/cache.py`.

## Considered Options
Reject a remote cache because it adds an outage domain.
